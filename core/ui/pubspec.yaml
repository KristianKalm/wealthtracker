name: kryptic_ui
description: "Reusable UI components for Kryptic apps."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.0
  image_cropper: ^11.0.0
  image: 4.5.4
  browser_image_compression: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
